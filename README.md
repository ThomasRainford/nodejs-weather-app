# nodejs-weather-app
A simple weather site using NodeJS and the Handlebars templating engine.

This app uses the Mapbox API to get the users location, then uses the Weatherstack API to get the weather data for given location.
